import styled from 'styled-components';

export const Wrapper = styled.div`
  position: relative;
  height: 100%;
`;

export const StyledButtonBackToCurrentMonth = styled.span`
  font-size: 16px;
  cursor: pointer;
  padding: 15px;
`;
